// import { fromJS } from 'immutable';
// import { selectBlockPageDomain } from '../selectors';

describe('selectBlockPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    // expect(true).toEqual(false);
    expect(false).toEqual(false);
  });
});
