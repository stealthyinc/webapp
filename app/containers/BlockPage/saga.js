// import { call, put, select, takeLatest } from 'redux-saga/effects';

/**
 * Root saga manages watcher lifecycle
 */
export default function* blockData() {
  // yield takeLatest(POLL_FOR_INVITES, pollForInvitesFun);
}
