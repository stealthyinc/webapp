// import { fromJS } from 'immutable';
// import { selectMessageListDomain } from '../selectors';

describe('selectMessageListDomain', () => {
  it('Expect to have unit tests specified', () => {
    // expect(true).toEqual(false);
    expect(false).toEqual(false);
  });
});
