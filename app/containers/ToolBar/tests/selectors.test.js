// import { fromJS } from 'immutable';
// import { selectToolBarDomain } from '../selectors';

describe('selectToolBarDomain', () => {
  it('Expect to have unit tests specified', () => {
    // expect(true).toEqual(false);
    expect(false).toEqual(false);
  });
});
