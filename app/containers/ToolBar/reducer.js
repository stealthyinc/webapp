/*
 *
 * ToolBar reducer
 *
 */

import {
} from './constants';

const initialState = {};

function toolBarReducer(state = initialState, action) {
  switch (action.type) {
    default:
      return state;
  }
}

export default toolBarReducer;
